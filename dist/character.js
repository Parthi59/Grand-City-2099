import {installScannedFace} from './human-face.js';
import {clothTexture,limbGeometry,makeHand} from './human-details.js';
import {installCivilianAsset} from './civilian-asset.js';
import * as T from './vendor/three.module.js';
import {installHumanAsset} from './human-asset.js';
const mix=(a,b,t)=>a+(b-a)*t;
const mix3=(a,b,t)=>a.map((v,i)=>mix(v,b[i],t));
const up=new T.Vector3(0,1,0);
export function makeHuman({color='#d2d8d3',skin='#ae795a',lowDetail=false,hero=false,helmet=false,variant=0,civilian=false}={}){
 const group=new T.Group(),rig=new T.Group();group.add(rig);if(lowDetail){const shadow=new T.Mesh(new T.CircleGeometry(.36,12),new T.MeshBasicMaterial({color:'#172128',transparent:true,opacity:.20,depthWrite:false}));shadow.rotation.x=-Math.PI/2;shadow.position.y=.009;shadow.scale.y=.7;shadow.renderOrder=-10;group.add(shadow);}
 const mat=(c,r=.8)=>new T.MeshStandardMaterial({color:c,roughness:r});
 const jacket=mat(color,.64),pants=mat(['#293541','#494038','#283d49','#343234','#51544a','#354453'][variant%6]),skinMat=mat(skin),black=mat('#131d26'),hair=mat(['#292724','#443326','#201e1c','#605446','#292724','#343330'][variant%6]),accent=mat('#afe3d0'),eyes=mat('#242821');
 jacket.map=clothTexture();pants.map=jacket.map;
 const sphereGeo=new T.SphereGeometry(1,lowDetail?14:24,lowDetail?10:18),cylinderGeo=new T.CylinderGeometry(.74,1,1,lowDetail?12:20);
 const gunSocket=new T.Group();rig.add(gunSocket);
 const ball=(pos,scale,m,parent=rig)=>{const o=new T.Mesh(sphereGeo,m);o.position.set(...pos);o.scale.set(...scale);parent.add(o);return o};
 const rod=(radius,m)=>{const o=new T.Mesh(cylinderGeo,m);o.userData.radius=radius;rig.add(o);return o};
 const segment=(m,a,b)=>{const av=new T.Vector3(...a),bv=new T.Vector3(...b),d=bv.clone().sub(av);m.position.copy(av.add(bv).multiplyScalar(.5));m.scale.set(m.userData.radius,d.length(),m.userData.radius);m.quaternion.setFromUnitVectors(up,d.normalize())};
 const pelvis=ball([0,.93,0],[.175,.145,.125],pants),torso=ball([0,1.25,0],[.215,.275,.135],jacket),chest=ball([0,1.41,-.012],[.228,.10,.135],jacket),neck=ball([0,1.55,0],[.052,.075,.057],skinMat);
 const bodyGeo=new T.BufferGeometry(),bp=[],bi=[],bn=lowDetail?16:28;const profiles=[[-.26,.168,.118],[-.19,.166,.12],[-.06,.18,.128],[.10,.215,.141],[.21,.228,.129],[.25,.175,.094],[.285,.078,.063]];for(const [y,rx,rz]of profiles)for(let j=0;j<=bn;j++){const a=j/bn*Math.PI*2;bp.push(Math.sin(a)*rx,y,Math.cos(a)*rz);}for(let i=0;i<profiles.length-1;i++)for(let j=0;j<bn;j++){const a=i*(bn+1)+j,b=a+bn+1;bi.push(a,a+1,b,a+1,b+1,b);}bodyGeo.setAttribute('position',new T.Float32BufferAttribute(bp,3));const buv=[];for(let i=0;i<profiles.length;i++)for(let j=0;j<=bn;j++)buv.push(j/bn,i/(profiles.length-1));bodyGeo.setAttribute('uv',new T.Float32BufferAttribute(buv,2));bodyGeo.setIndex(bi);bodyGeo.computeVertexNormals();torso.geometry=bodyGeo;torso.scale.set(1,1,1);chest.visible=false;
 const head=new T.Group(),faceFallback=new T.Group();rig.add(head);head.add(faceFallback);
 const faceGeo=new T.SphereGeometry(1,lowDetail?20:32,lowDetail?16:24),fp=faceGeo.attributes.position;
 for(let i=0;i<fp.count;i++){let x=fp.getX(i)*.091,y=fp.getY(i)*.123,z=fp.getZ(i)*.091;const low=Math.max(0,-y/.123);x*=1-low*.18;if(z<0){const front=Math.max(0,-z/.091),g=(cx,cy,sx,sy)=>Math.exp(-(((x-cx)/sx)**2+((y-cy)/sy)**2));z-=front*(.023*g(0,-.004,.017,.028)+.008*g(0,-.052,.037,.015));z+=.007*(g(.033,.026,.019,.015)+g(-.033,.026,.019,.015));}fp.setXYZ(i,x,y,z);}faceGeo.computeVertexNormals();const face=new T.Mesh(faceGeo,skinMat);faceFallback.add(face);
 const hairGeo=new T.SphereGeometry(1,lowDetail?18:28,lowDetail?10:16,0,Math.PI*2,0,Math.PI*.47),hairMesh=new T.Mesh(hairGeo,hair);hairMesh.scale.set(.094,.132,.095);hairMesh.position.set(0,.004,.006);hairMesh.rotation.x=-.15;head.add(hairMesh);hairMesh.scale.set(.085,.126,.098);hairMesh.position.set(0,.008,.006);
 if(helmet){ball([0,.013,0],[.109,.137,.111],mat('#303c45',.3),head);ball([0,.015,-.091],[.098,.053,.029],mat('#111e28',.15),head);ball([0,-.060,-.071],[.092,.040,.071],mat('#444e52',.3),head);}
 const lips=mat('#815e51'),sclera=mat('#c7bfb0');ball([0,-.054,-.087],[.027,.0035,.005],lips,faceFallback);
 for(const side of [-1,1]){ball([side*.09,-.002,.008],[.015,.029,.018],skinMat,faceFallback);ball([side*.099,-.002,.004],[.004,.017,.009],lips,faceFallback);ball([side*.034,.026,-.082],[.013,.0065,.005],sclera,faceFallback);ball([side*.034,.026,-.087],[.005,.005,.0015],eyes,faceFallback);const brow=ball([side*.034,.040,-.079],[.019,.003,.005],hair,faceFallback);brow.rotation.z=side*.10;}
 const zipper=rod(.0035,black),collar=ball([0,1.48,-.025],[.079,.022,.066],black),belt=ball([0,.96,0],[.197,.026,.15],black);
 zipper.visible=false;const zipCurve=new T.CatmullRomCurve3([new T.Vector3(0,-.20,-.124),new T.Vector3(0,-.06,-.130),new T.Vector3(0,.10,-.143),new T.Vector3(0,.21,-.132)]);const zipMesh=new T.Mesh(new T.TubeGeometry(zipCurve,16,.0025,5,false),black);torso.add(zipMesh);
 const backpack=ball([0,1.26,.142],[.085,.11,.007],black),stripe=rod(.003,accent);backpack.visible=false;stripe.visible=false;
 const seams=new T.Group();torso.add(seams);const seamMat=mat('#89928f');for(const side of[-1,1]){const pocket=new T.Mesh(new T.BoxGeometry(.075,.0025,.002),seamMat);pocket.position.set(side*.098,-.10,-.124);pocket.rotation.z=side*.30;seams.add(pocket);}const hem=new T.Mesh(new T.TorusGeometry(.16,.008,5,28),jacket);hem.rotation.x=Math.PI/2;hem.scale.y=.72;hem.position.y=-.244;seams.add(hem);
 const phone=new T.Mesh(new T.BoxGeometry(.06,.115,.009),mat('#18232d',.35));rig.add(phone);phone.visible=false;const legs=[],arms=[];
 for(const side of [-1,1]){
  const leg={side,upper:rod(.099,pants),lower:rod(.075,pants),knee:ball([0,0,0],[.071,.066,.069],pants),foot:ball([0,0,0],[.060,.047,.133],black)};leg.upper.geometry=limbGeometry('thigh',lowDetail);leg.lower.geometry=limbGeometry('shin',lowDetail);legs.push(leg);
  const hand=makeHand(skinMat,side,sphereGeo);rig.add(hand);const arm={side,shoulder:ball([0,0,0],[.071,.061,.071],jacket),upper:rod(.077,jacket),lower:rod(.053,jacket),elbow:ball([0,0,0],[.057,.052,.055],jacket),hand};arm.upper.geometry=limbGeometry('sleeve',lowDetail);arm.lower.geometry=limbGeometry('forearm',lowDetail);arms.push(arm);
 }
 function kneeIK(a,b,l1,l2,bend){const av=new T.Vector3(...a),bv=new T.Vector3(...b),dir=bv.clone().sub(av),d=Math.min(l1+l2-.001,Math.max(.01,dir.length()));dir.normalize();const base=(l1*l1-l2*l2+d*d)/(2*d),lift=Math.sqrt(Math.max(0,l1*l1-base*base));const n=new T.Vector3(...bend);n.addScaledVector(dir,-n.dot(dir)).normalize();return av.addScaledVector(dir,base).addScaledVector(n,lift).toArray();}
 function pose({time=0,phase=0,speed=0,sprinting=false,seated=0,kind='bike',seat=.81,transition=0,turn=0,weapon=null,aiming=false,aimPitch=0,reload=0,recoil=0,fallen=0,flinch=0,slump=0,activity='walk',detail=true}={}){
  rig.rotation.z=fallen*Math.PI*.48+slump*.23;rig.rotation.x=slump*.28+flinch*.10;rig.position.y=fallen*.12-slump*.08;
  const moving=Math.min(1,speed/1.7),run=Math.min(1,speed/7),breath=Math.sin(time*2.2)*.004;
  const hipY=mix(.92+Math.abs(Math.sin(phase))*moving*.023+breath,kind==='car'?seat:seat+.025,seated);
  const forward=mix(-run*.05,kind==='car'?.02:-.28,seated);
  pelvis.position.set(0,hipY,0);belt.position.set(0,hipY+.045,0);torso.position.set(0,hipY+.30,forward*.5);torso.rotation.x=mix(-run*.08,kind==='car'?.10:-.38,seated);chest.position.set(0,hipY+.48,forward);neck.position.set(0,hipY+.62,forward-.018);
  const topY=hipY+.30+Math.cos(torso.rotation.x)*.285,topZ=forward*.5+Math.sin(torso.rotation.x)*.285;collar.position.set(0,topY,topZ);neck.position.set(0,topY+.045,topZ-.003);head.position.set(0,topY+.160,topZ-.020);head.rotation.y=-turn*.12+(moving?0:Math.sin(time*.65+variant)*.10);head.rotation.x=activity==='phone'?.12:0;backpack.position.set(0,hipY+.32,forward*.5+.16);segment(zipper,[0,hipY+.16,forward*.4-.145],[0,hipY+.50,forward-.145]);segment(stripe,[-.04,hipY+.23,forward*.5+.154],[-.04,hipY+.39,forward*.5+.154]);
  for(const l of legs){const q=phase+(l.side>0?Math.PI:0),swing=Math.sin(q),stride=.30+run*.16;const walk=[l.side*.12,.07+Math.max(0,Math.cos(q))*(.09+run*.09)*moving,swing*stride*moving];
   let target=kind==='car'?[l.side*.15,.18,-.59]:[l.side*.29,.43,-.075];const ankle=mix3(walk,target,seated);ankle[2]+=fallen*l.side*.15;
   if(kind==='bike'&&l.side>0&&transition>0)ankle[1]+=Math.sin(transition*Math.PI)*.57;
   const hip=[l.side*.125,hipY,0],knee=kneeIK(hip,ankle,.44,.43,[0,0,-1]);segment(l.upper,hip,knee);segment(l.lower,knee,ankle);l.knee.position.set(...knee);l.foot.position.set(ankle[0],ankle[1]-.015,ankle[2]-.055);l.foot.rotation.x=seated?.18:Math.min(0,Math.cos(q))*.1*moving;
  }
  for(const a of arms){const q=phase+(a.side>0?0:Math.PI),shoulder=[a.side*.25,hipY+.30+Math.cos(torso.rotation.x)*.195,forward*.5+Math.sin(torso.rotation.x)*.195];const walk=[a.side*.29,hipY-.04+run*.12,Math.sin(q)*(.20+run*.13)*moving-.025];const target=kind==='car'?[a.side*.155,hipY+.38,-.58]:[a.side*.36,seat<.75?1.04:1.01,seat<.75?-.62:-.71];let wrist=mix3(walk,target,seated);if(weapon&&!seated){const long=weapon==='carbine';wrist=a.side>0?[.20,hipY+(aiming?.46:.35)-recoil*1.1,-(long?.40:.43)+recoil]:[long?.06:.17,hipY+(aiming?.44:.33),long?-.70:-.44];wrist[1]-=Math.sin(aimPitch)*.5;if(reload){wrist[1]-=Math.sin(reload*Math.PI)*.22;wrist[2]+=.13*Math.sin(reload*Math.PI);}}if(transition>.05&&transition<.7&&a.side>0&&kind==='car'){wrist[0]+=.30*Math.sin(transition*Math.PI);wrist[1]+=.16*Math.sin(transition*Math.PI);}
   if(!seated&&!weapon){if(activity==='phone'&&a.side>0)wrist=[.16,hipY+.40,-.25];if(activity==='talk')wrist=[a.side*(.25+.045*Math.sin(time*1.8)),hipY+.14+.07*Math.sin(time*1.6+a.side),-.24];}if(fallen){wrist[0]+=a.side*fallen*.22;wrist[2]+=a.side*fallen*.20;}if(flinch){wrist[1]+=flinch*.18;wrist[2]-=flinch*.12;}const elbow=kneeIK(shoulder,wrist,.30,.275,[a.side*.5,0,1]);a.shoulder.position.set(...shoulder);segment(a.upper,shoulder,elbow);segment(a.lower,elbow,wrist);a.elbow.position.set(...elbow);a.hand.position.set(...wrist);a.hand.rotation.x=seated||weapon?Math.PI/2:.12;a.hand.rotation.z=a.side*.07;if(a.side>0){phone.visible=activity==='phone'&&!fallen;phone.position.set(wrist[0],wrist[1]+.04,wrist[2]-.021);}if(a.side>0){gunSocket.position.set(wrist[0],wrist[1]+.035,wrist[2]-.035);gunSocket.rotation.set(-aimPitch+recoil*1.7,reload?Math.sin(reload*Math.PI)*.25:0,reload?-.32*Math.sin(reload*Math.PI):0);}
  }
 }
 group.traverse(m=>{if(m.isMesh){m.castShadow=true;m.userData.dynamic=true}});pose();let asset=null,faceAsset=null;const api={group,pose:args=>{pose(args);asset?.update(args);faceAsset?.update(args);},gunSocket,ready:Promise.resolve()};if(!civilian&&!helmet&&typeof window!=='undefined')api.ready=installScannedFace({head,fallback:faceFallback,hairMesh,skinMat,variant,lowDetail}).then(a=>{faceAsset=a;a.update();}).catch(error=>console.warn('Face detail unavailable',error));if(civilian&&typeof window!=='undefined')api.ready=installCivilianAsset({group,rig,variant}).then(a=>asset=a).catch(error=>console.warn('Civilian detail unavailable',error));if(hero&&typeof window!=='undefined')api.ready=installHumanAsset({group,rig,head,gunSocket}).then(a=>{asset=a;}).catch(error=>{console.warn('Detailed character unavailable; using local character.',error);});return api;
}
